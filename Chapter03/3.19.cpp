#include <vector>

using std::vector;

int main() {
	vector<int> ivec1(10, 42); // preferred way: shorter and more understandable
	vector<int> ivec2 = {42, 42, 42, 42, 42, 42, 42, 42, 42, 42};
	vector<int> ivec3;
	for (unsigned cnt = 0; u != 10; ++u) {
		ivec3.push_back(42);
	}
	return 0;
}
