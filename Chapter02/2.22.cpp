#include <iostream>

int main() {
	int *p = nullptr;
	if (p)	// if pointer has any non-null memory address assigned to it
	if (*p)	// if the object which the pointer points to has a nonzero value
	return 0;
}
